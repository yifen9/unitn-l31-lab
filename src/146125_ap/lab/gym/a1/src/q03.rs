/*
Write a function `multiply` that takes an `i32`, a `f32` and a `f64`
and returns the multiplication of the three of them as a `f64` value;
*/

fn multiply(x: i32, y: f32, z: f64) -> f64 {
    x as f64 * y as f64 * z
}
