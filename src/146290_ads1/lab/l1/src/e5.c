/*
index search in an array

Implement a C function that takes two inputs:
a dynamically allocated long integer array containing MAX_SIZE=10 elements
and a variable called target of the same data type.

The function must sequentially search for this value in the array, set it to
zero, and return the index of the first occurrence of target in the input array.

Then implement a recursive version of the same function. Write a fully working
example code, not just the function declaration.
*/